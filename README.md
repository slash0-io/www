# slash0.io

The slash0 landing page — a single static file served by GitHub Pages
(deploy from branch, `main` / root) at https://slash0.io.

No build system on purpose. Edit `index.html`, push, done.
